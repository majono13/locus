namespace Locus.Domain.Enums
{
    public enum PropertyTypeEnum
    {
        House = 0,                 // Casa
        Apartment = 1,             // Apartamento
        Penthouse = 2,             // Cobertura
        Duplex = 3,                // Duplex
        Loft = 4,                  // Loft
        Studio = 5,                // Studio
        CommercialRoom = 6,       // Sala comercial
        Office = 7,               // Escritório
        Store = 8,                // Loja
        Warehouse = 9,            // Galpão / Armazém
        Shed = 10,                 // Barracão
        CommercialBuilding = 11,   // Prédio comercial
        LandLot = 12,              // Terreno / Lote
        Farm = 13,                 // Fazenda
        Site = 14,                 // Sítio
        Ranch = 15,                // Rancho
        VacationHome = 16,         // Casa de temporada
        Room = 17,                 // Quarto individual
        Other = 18                 // Outro / Não especificado
    }
}
